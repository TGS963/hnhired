{
  "name": "hn-hired-ai",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "ingest": "npm run -w @hnhired/ingest start",
    "web:dev": "npm run -w @hnhired/web dev",
    "web:build": "npm run -w @hnhired/web build",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate.mjs"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.10"
  },
  "engines": {
    "node": ">=20"
  }
}
